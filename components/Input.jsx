import styles from ""

export default function Input( {input} ) {
    return (
        <div className={styles.login}>
            <input type="text" />
            <input type="text" />
        </div>
    )
}